// import * as actionTypes from "../actions/actions";

// const spinnerReducer = (state = [], action) => {
//   switch (action.type) {
//     case actionTypes.INIT_SPINNERS:
//       const spinner = action.data;
//       return spinner;
//     default:
//       return state;
//   }
// };

// export default spinnerReducer;